#include <stdio.h>
#include <cs50.h>

int main(void){
    int nums [] = {7,6,3,14,0,12,1,99};
    //method for array size
    int length = sizeof(nums) / sizeof(nums[0]);
    for (int i = 0; i <= length; i++){
        //if a0(7) > a1(6) x = a0 a0 = a1 a1 = x

    }

}

//bubble sorting is comparing element to neighbour and swap them

//recursion - if function calls itself
